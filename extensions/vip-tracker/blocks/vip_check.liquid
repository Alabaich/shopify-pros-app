{% if customer %}
  <script>
    document.addEventListener("DOMContentLoaded", function() {
      const customerId = "{{ customer.id }}";
      const proxyUrl = "/apps/vip-status/check?customerId=" + customerId;
      fetch(proxyUrl).catch(() => {});
    });
  </script>
{% endif %}

{% schema %}
{
  "name": "VIP Tracker Script",
  "target": "body",
  "settings": []
}
{% endschema %}